#include <stdio.h>
/**
 * main - print uppercase letters
 * Return: always 0
 */
int main(void)
{
	char Lc;
	char lc;
	for (lc = 'a'; lc <= 'z'; lc++);
	{
		putchar(lc)
	}
	for (Lc = 'A'; Lc <= 'Z'; lc++);
	{
		putchar(Lc);
	}
	putchar('\n');
	Return(0)
}
